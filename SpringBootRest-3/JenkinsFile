pipeline {
    agent any
    tools {
         maven 'maven3'
    }
    stages{
        stage('Build'){
            steps{
                  //sh script: 'mvn clean package'
                  cleanWs()
                  checkout scm
                  echo "Building ${env.project1}..."
            }
        }
        }
        }
